name: Test React App
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v7
      - name: Install dependecies
        run: npm ci #npm install but safer
      - name: Test application
        run: npm run test
